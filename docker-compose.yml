services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: channelsignal
      POSTGRES_PASSWORD: channelsignal
      POSTGRES_DB: channelsignal
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
